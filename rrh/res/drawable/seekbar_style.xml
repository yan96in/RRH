<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="1dip" />

            <gradient
                android:angle="90"
                android:centerColor="@color/detail_progress_background"
                android:centerY="0.45"
                android:endColor="@color/detail_progress_background"
                android:startColor="@color/detail_progress_background" />
        </shape>
    </item>
<!--     <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="1dip" />

                <gradient
                    android:angle="270"
                    android:centerColor="@color/price"
                    android:centerY="0.45"
                    android:endColor="@color/price"
                    android:startColor="@color/price" />
            </shape>
        </clip>
    </item> -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="1dip" />

                <gradient
                    android:angle="90"
                    android:centerColor="@color/orange_little"
                    android:centerY="0.45"
                    android:endColor="@color/orange_little"
                    android:startColor="@color/orange_little" />
            </shape>
        </clip>
    </item>

</layer-list>